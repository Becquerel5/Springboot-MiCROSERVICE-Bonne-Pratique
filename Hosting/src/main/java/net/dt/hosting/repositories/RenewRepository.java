package net.dt.hosting.repositories;

import net.dt.hosting.entities.Renew;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RenewRepository extends JpaRepository<Renew,String> {
}
