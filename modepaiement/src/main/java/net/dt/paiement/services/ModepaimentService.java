package net.dt.paiement.services;

import net.dt.paiement.dtos.ModepaimentRequestDTO;
import net.dt.paiement.dtos.ModepaimentResponseDTO;

public interface ModepaimentService extends GeneralService<ModepaimentRequestDTO, ModepaimentResponseDTO> {
}
